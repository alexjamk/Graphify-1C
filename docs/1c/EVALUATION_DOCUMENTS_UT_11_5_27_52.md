# Проверка на двух документах «Управление торговлей» 11.5.27.52

Дата: 18 сентября 2026 года. Основная конфигурация `./src/cf`; расширения только `Расширение 1` и `Расширение 2`. Проверен граф после добавления разрешения методов менеджера регистра: 552 278 узлов, 1 222 312 связей. SQL-запросы выполнялись к `graphify-out/ut-11-5-27-52.sqlite`; эталон проверялся отдельно по XML и BSL.

В выгрузке документ, соответствующий «поступлению на расчётный счёт», называется `ПоступлениеБезналичныхДенежныхСредств`.

| № | Вопрос | Эталон по исходникам | Ответ графа |
| --- | --- | --- | --- |
| 1 | Какие из выбранных расширений заимствуют `ПоступлениеБезналичныхДенежныхСредств`? | Оба: `ObjectBelonging=Adopted` в XML обоих расширений, строка 31. | Две связи `EXTENDS`, верно. |
| 2 | Какие из выбранных расширений заимствуют `ЗаказКлиента`? | Оба: XML расширений, строки 31 и 27. | Две связи `EXTENDS`, верно. |
| 3 | Какие методы поступления перехвачены? | В `Расширение 2` стоят два `&После`: `ОбработкаПроведения` и `ПередЗаписью`, строки 2 и 10 модуля объекта. | Две связи `AFTER` к соответствующим базовым методам, верно. |
| 4 | Есть ли перехваты методов заказа клиента в двух выбранных расширениях? | Поиск четырёх директив в BSL каталогов `Documents/ЗаказКлиента` обоих расширений не дал совпадений. | Связей `BEFORE/AFTER/INSTEAD/CHANGE_CONTROL` к методам заказа нет, верно в указанной области поиска. |
| 5 | Какие методы напрямую вызывает `ОбработкаПроведения` поступления? | `ПроведениеДокументов.ОбработкаПроведенияДокумента` и `ПоступлениеБезналичныхДенежныхСредствЛокализация.ОбработкаПроведения`, строки 441–442. | Две связи `calls` к этим методам, верно. |
| 6 | Какие межмодульные методы напрямую вызывает `ОбработкаПроведения` заказа? | Пять вызовов: `ПроведениеДокументов`, `ЗаказКлиентаЛокализация`, `ДоставкаТоваров`, менеджер регистра `СтатусыСборкиИДоставки`, `ПродажиСервер`; строки 857–865. | Пять связей `calls`, верно после исправления resolver. |
| 7 | Куда ведёт вызов `ЗаписатьСтатусИзРаспоряжения` из проведения заказа? | В экспортный метод модуля менеджера регистра `СтатусыСборкиИДоставки`, строка 73. | Цель разрешена как `InformationRegister/.../ManagerModule/ЗаписатьСтатусИзРаспоряжения`, верно. |
| 8 | Можно ли по графу доказать запись в `СтатусыСборкиИДоставки` из проведения заказа? | Менеджер вызывает `ЗаписатьСтатус` (строки 121 и 128), тот создаёт менеджер записи (142) и выполняет `ЗаписьРегистра.Записать(Истина)` (153). | Граф показывает цепочку `calls`, но **не показывает `writes`** через переменную `ЗаписьРегистра`. Ответ неполный. |
| 9 | Какой тип у реквизита `БанковскийСчет` поступления? | `CatalogRef.БанковскиеСчетаОрганизаций`, XML строки 386 и 395. | `type_reference` к `Catalog/БанковскиеСчетаОрганизаций`, верно. |
| 10 | Какой тип у реквизита `ГрафикОплаты` заказа? | `CatalogRef.ГрафикиОплаты`, XML строки 650 и 659. | `type_reference` к `Catalog/ГрафикиОплаты`, верно. |

Эталон проверялся по исходникам локальной выгрузки; сама выгрузка не публикуется.

Этот набор выбран вручную и содержит близкие вопросы, поэтому долю 9/10 нельзя переносить на проект в целом. Особенно важен вопрос 8: наличие пути вызовов не равнозначно доказанной записи. Для следующего этапа нужен перенос значения регистра через переменную менеджера записи и повторная проверка этого сценария.

## Повторная проверка после исправления

18 сентября 2026 года граф пересобран после добавления локального отслеживания переменной менеджера записи. Метод `1c://InformationRegister/СтатусыСборкиИДоставки/ManagerModule/ЗаписатьСтатус` теперь имеет связь `writes` к `1c://InformationRegister/СтатусыСборкиИДоставки` с уверенностью `INFERRED`. Повторный запуск `scripts/benchmark_onec_documents.py` дал совпадение ответов по **всем 10 вопросам**. Исходная таблица выше оставлена как история обнаружения ошибки; замеры времени и токенов в соседних отчетах относятся к предыдущему графу. Это локальный паттерн присваивания и вызова, а не полный анализ потока данных BSL.
